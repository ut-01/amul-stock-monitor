{
  "name": "amul-stock-monitor",
  "version": "1.0.0",
  "description": "Amul Stock Monitor using Playwright",
  "main": "index.js",
  "private": true,
  "scripts": {
    "start": "node index.js",
    "init": "npm i; npx playwright install chromium; npx playwright install-deps chromium",
    "dev": "NODE_ENV=development node index.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "playwright": "^1.55.0"
  },
  "engines": {
    "node": ">=20"
  }
}